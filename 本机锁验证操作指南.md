# 本机锁验证操作指南

在**无需 GitHub、Docker 或 Runner 部署**的情况下，仅用本机终端验证 runner-wrapper 的锁机制是否生效。

---

## 前提条件

- 已获取 `runner-wrapper.sh` 脚本
- 系统提供 `flock`（通常随 `util-linux`，Linux 一般已预装）

---

## 第一步：获取 runner-wrapper.sh

```bash
# 进入工作目录
cd ~/runner-validation   # 或任意目录
mkdir -p runner-wrapper
cd runner-wrapper

# 从 github-runners 仓库获取（任选其一）
# 方式 A：克隆仓库
git clone https://github.com/arceos-hypervisor/github-runners.git temp && cp temp/runner-wrapper/runner-wrapper.sh . && rm -rf temp

# 方式 B：若已有 github-runners clone
cp /path/to/github-runners/runner-wrapper/runner-wrapper.sh .

chmod +x runner-wrapper.sh
```

---

## 第二步：创建锁目录

```bash
mkdir -p /tmp/github-runner-locks
```

---

## 第三步：打开两个终端进行验证

### 终端 1：获取锁并长时间占用

```bash
cd ~/runner-validation/runner-wrapper

# 使用 sleep 模拟长时间任务（999 秒）
RUNNER_RESOURCE_ID=test-lock RUNNER_SCRIPT=/bin/sleep ./runner-wrapper.sh 999
```

**预期输出**：

```
[2026-02-08T...] ⏳ Waiting for lock: test-lock
[2026-02-08T...]    Requested by: unknown/unknown
[2026-02-08T...] ✅ Acquired lock for test-lock
[2026-02-08T...]    Owner: unknown/unknown
[2026-02-08T...]    Started at: ...
```

此时脚本会阻塞（sleep 999 秒），保持终端 1 不关闭。

### 终端 2：尝试获取同一把锁（会阻塞）

在**另一个终端**执行：

```bash
cd ~/runner-validation/runner-wrapper

RUNNER_RESOURCE_ID=test-lock RUNNER_SCRIPT=/bin/echo ./runner-wrapper.sh "Got lock!"
```

**预期输出**（会先卡住）：

```
[2026-02-08T...] ⏳ Waiting for lock: test-lock
[2026-02-08T...]    Requested by: unknown/unknown
```

终端 2 会一直停在 "Waiting for lock"，直到终端 1 释放锁。

### 终端 1：释放锁

在终端 1 按 `Ctrl+C` 结束 sleep 进程。

**终端 1 预期输出**：

```
[2026-02-08T...] 🔓 Releasing lock for test-lock
```

### 终端 2：获得锁并继续

终端 1 释放锁后，终端 2 会立即获得锁并执行：

```
[2026-02-08T...] ✅ Acquired lock for test-lock
[2026-02-08T...]    Owner: unknown/unknown
[2026-02-08T...]    Started at: ...
Got lock!
[2026-02-08T...] 🔓 Releasing lock for test-lock
```

---

## 第四步：验证结论

| 现象 | 说明 |
|------|------|
| 终端 2 在终端 1 持有锁时一直阻塞 | ✅ 锁生效，后者等待前者 |
| 终端 1 释放后终端 2 立即获得锁 | ✅ 锁正确释放 |
| 两终端从未同时输出 "Acquired lock" | ✅ 互斥正确 |

若符合上述现象，说明 runner-wrapper 的锁机制在本机工作正常。

---

## 简化版（单终端后台执行）

若不想开两个终端，可用后台进程模拟：

```bash
cd ~/runner-validation/runner-wrapper

# 后台启动，占用锁 10 秒
RUNNER_RESOURCE_ID=test-lock RUNNER_SCRIPT=/bin/sleep ./runner-wrapper.sh 10 &
sleep 2

# 前台执行，会阻塞直到上一个结束
RUNNER_RESOURCE_ID=test-lock RUNNER_SCRIPT=/bin/echo ./runner-wrapper.sh "Done"
```

---

## 常见问题

| 问题 | 处理 |
|------|------|
| `flock: command not found` | 安装 util-linux：`sudo apt install util-linux` |
| `Runner script not found` | 显式设置 `RUNNER_SCRIPT`，如 `RUNNER_SCRIPT=/bin/sleep` |
| 锁目录无写权限 | `sudo chmod 1777 /tmp/github-runner-locks` |

---

## 参考资料

- [多组织共享测试环境实施文档](多组织共享测试环境实施文档.md)
- [多组织Runner锁验证操作指南](多组织Runner锁验证操作指南.md)（需 GitHub 的完整验证）
